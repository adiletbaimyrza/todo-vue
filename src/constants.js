export const TodoFilter = Object.freeze({
  ALL: 'all',
  ACTIVE: 'active',
  COMPLETED: 'completed',
})

export const Theme = Object.freeze({
  TO_STRING: 'theme',
  LIGHT: 'light',
  DARK: 'dark',
})
